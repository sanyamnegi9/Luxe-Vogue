import React from "react";

const Loading = () => {
  return (
    <div className="box flex h-[calc(100vh-4rem)] w-full items-center justify-center">
      <div className="coin" />
    </div>
  );
};

export default Loading;
